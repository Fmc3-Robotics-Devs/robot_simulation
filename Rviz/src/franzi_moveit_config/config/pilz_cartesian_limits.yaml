# Conservative cartesian limits for Pilz PTP, LIN, and CIRC planning.
# Validate these against the physical controller before allowing execution.
cartesian_limits:
  max_trans_vel: 0.25
  max_trans_acc: 0.50
  max_trans_dec: -0.50
  max_rot_vel: 0.50
